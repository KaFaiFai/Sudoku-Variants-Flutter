// ignore_for_file: constant_identifier_names

import 'package:flutter/material.dart';

const MaterialColor PRIMARY_COLOR = Colors.blueGrey;
const MaterialColor ACCENT_COLOR = Colors.green;
const MaterialColor CONTRAST_COLOR = Colors.orange;
const MaterialColor TERTIARY_COLOR = Colors.grey;

const double LARGE_MARGIN = 50;
const double SMALL_MARGIN = 5;

const Duration TRANSITION_DURATION = Duration(milliseconds: 300);
